# Example input contract for the public training entry point.
# Replace every placeholder before sourcing this file.
export DATASET_BASE_PATH=/path/to/robotwin_training
export TRAINING_MANIFEST=/path/to/train.jsonl
export TRAINING_MANIFEST_SHA256=YOUR_64_HEX_SHA256
export RESUME_CHECKPOINT=/path/to/compatible_warm_start.safetensors
export RESUME_CHECKPOINT_SHA256=YOUR_64_HEX_SHA256
export NUM_GPUS=1
export OUTPUT_PATH=/path/to/output

# Reference code defaults to head-camera, robot-only flow, and a memory gate
# calibrated for the original 64-GiB accelerator class.
